name: Upload Preview APK

on:
  pull_request:
  workflow_dispatch:
    inputs:
      abi_filter:
        description: 'ABI Filter'
        required: false
        default: 'arm64-v8a'
        type: choice
        options:
          - arm64-v8a
          - armeabi-v7a
          - x86
          - x86_64

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  build:
    name: Upload Preview APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: 'temurin'

      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - uses: android-actions/setup-android@v3
      
      - uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r27

      # 🔧 CORREGIDO: Setup Rust sin actions-rs (más confiable)
      - name: Setup Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          source "$HOME/.cargo/env"

      # 🔧 CORREGIDO: Añadir targets de Android uno por uno
      - name: Add Android targets
        run: |
          rustup target add aarch64-linux-android
          rustup target add armv7-linux-androideabi
          rustup target add i686-linux-android
          rustup target add x86_64-linux-android

      # 🔧 NUEVO: Instalar cargo-ndk
      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: jni/nexuschat-core-rust

      - name: Get nexuschat-core-rust submodule hash
        id: core-hash
        run: echo "hash=$(git rev-parse HEAD:jni/nexuschat-core-rust)" >> $GITHUB_OUTPUT

      - name: Restore compiled core
        id: core-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            jni/arm64-v8a
            libs/arm64-v8a
          key: core-arm64-v8a-${{ steps.core-hash.outputs.hash }}-${{ hashFiles('jni/Android.mk', 'jni/Application.mk', 'jni/nc_wrapper.c', 'scripts/ndk-make.sh') }}-ndk-r27

      # 🔧 CORREGIDO: Dar permisos a los scripts antes de ejecutarlos
      - name: Compile core
        if: steps.core-cache.outputs.cache-hit != 'true'
        env:
          ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          # Dar permisos de ejecución a los scripts
          chmod +x scripts/install-toolchains.sh
          chmod +x scripts/ndk-make.sh
          
          # Añadir NDK al PATH
          export PATH="${PATH}:${ANDROID_NDK_ROOT}/toolchains/llvm/prebuilt/linux-x86_64/bin/"
          
          # Ejecutar scripts con bash (por si el shebang falla)
          bash scripts/install-toolchains.sh
          bash scripts/ndk-make.sh arm64-v8a

      # 🔧 CORREGIDO: Dar permisos a gradlew
      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon -PABI_FILTER=${{ github.event.inputs.abi_filter || 'arm64-v8a' }} assembleGplayDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-preview.apk
          path: 'build/outputs/apk/gplay/debug/*.apk'